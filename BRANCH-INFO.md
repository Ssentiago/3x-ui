# feat/group-tariff-profiles — сводка

База: `ece16559 fix(docs): prevent theme switch hydration mismatch`

## Разбивка diff

| Категория | Файлов | +строк | −строк | Что читать |
|-----------|--------|--------|--------|------------|
| Core/Backend (Go) | 24 | 2 711 | 359 | Обязательно |
| Frontend (React) | 28 | 2 444 | 673 | Обязательно |
| Go тесты | 16 | 5 521 | 26 | По желанию |
| Frontend тесты | 13 | — | — | По желанию |
| i18n (13 локалей) | 13 | 1 170 | 53 | По желанию |
| Generated (openapi, zod, types, msw) | 7 | 5 883 | 2 224 | **Пропустить** |
| **Итого** | **101** | **17 729** | **3 335** | |

---

## ce7cce37 chore: update generated OpenAPI spec, Zod schemas, types, and MSW

Auto-generated by `make gen`

- frontend/public/openapi.json: new tariff/group/profile endpoints
- frontend/src/generated/{schemas,types,zod,examples}.ts: TS types
  and Zod validators mirrored from Go structs
- frontend/public/mockServiceWorker.js: updated service worker
- docs/public/openapi.json: synced copy for Fumadocs API reference


## c02957d3 test(tariff): full coverage for tariff/group/profile resolution chain + i18n

## Go backend tests (14 new files)
- TariffService CRUD + rewriteTrafficForClients + SetProfiles (21 tests)
- ProfileService CRUD + validation + delete-blocked-by-tariffs (20)
- OverrideField / ReturnToTariff / resetClientOverrides (14)
- ResolveForGroup / GetClientTariffStartedAtMap / ClientBatchResolver (15)
- SyncInbound tariff-protection + tariffIdsContainingInbound (9)
- Sub-package AggregateTrafficByEmails + subscriptionExpiryFromClient (5)
- Controller HTTP tests for group/tariff/profile endpoints (19)
- Fill gaps: override flags clearing, SQL/Go divergence, partial override

## Frontend tests (8 new files + 7 updated)
- useTariffOverrides hook (10 tests)
- ManagedField + units.ts + strategies.ts (13)
- ClientFormModal tariff flow integration (2)
- TariffFormModal + ProfileFormModal + GroupFormModal (6)
- Updated snapshots for new tariff fields

## i18n (13 locales)
New keys: menu.tariffs, menu.groupList, pages.tariffs.*, pages.profiles.*,
pages.groups.* (tariffColumn, noTariff, removeTariff, etc),
pages.clients.* (managedFieldLocked, noActiveTariff, etc), saveSuccess.
Normalized indentation 4→2 spaces, keys at logical positions.


## 845c9186 feat(tariff): frontend — React components, modals, and tariff UI

## Groups page split into three tabs
- GroupsTab: group CRUD with tariff column, bulk add/remove, traffic reset
- TariffsTab: tariff CRUD with profile chain editor, live resolved preview,
  impact warning on edit/delete
- ProfilesTab: profile CRUD with inline editing

## New components
- ManagedField: displays tariff-resolved field values with source label,
  override indicator, and return-to-tariff button
- GroupFormModal, TariffFormModal, ProfileFormModal: create/edit forms
  with validation and tariff profile chain editor
- useTariffOverrides hook: tracks per-field override state

## Client form integration
- ClientFormModal: managed fields for totalGB/expiryTime/limitIp with
  tariff source display, override controls, and return-to-tariff actions
- ClientsPage: uses full client data from GET to overwrite list data on
  edit; tariff group selection resolves managed fields from server

## Schemas
- tariff.ts: Tariff, Profile, TariffProfile Zod schemas
- profile.ts: Profile form schemas
- client.ts: extended with tariff fields, GroupSummary with tariffId/tariffName

## Shared lib
- strategies.ts: profile chain merge strategies (overwrite/sum/union)
- units.ts: bytesPerGB constant, single source across codebase

## Sidebar & navigation
- AppSidebar: add Groups and Tariffs menu items with icons


## c036b653 feat(tariff): core backend — DB migrations, models, and Go services

Introduce Group/Tariff/Profile three-entity model with chain resolution.
Clients in a tariff-bound group receive tariff-resolved traffic/expiry/IP/
inbounds at read time via resolveChain, with per-client override/return.

## Models & DB
- Add Tariff, Profile, TariffProfile, ClientTariff, ClientGroup tables
- Override storage on client_tariffs row (total_gb_override, limit_ip_override,
  expiry_time_override, is_inbounds_overridden) with partial unique index
  WHERE ended_at IS NULL — scoped per tariff membership, auto-cleaned on leave
- Remove ClientRecord bool flags (is_total_gb_overridden etc.)
- Replace tariff_started_at column with client_tariffs table

## Services
- TariffService: CRUD, profile chain management, rewriteTrafficForClients,
  ResolveTariffInboundMap, live ApplyInboundList via gRPC Attach/Detach
- ProfileService: CRUD with validation, delete-blocked-by-tariffs check
- ClientTariffService: activateClientTariffsByEmails with batch INSERT,
  OverrideField/ReturnToTariff on CT row, live inbound diff on group change
- Client resolution (client_resolve.go): resolveChain, ResolveClientFields,
  BatchResolveEffectiveInboundIds, FieldMode enum (Tariff/Override/Own)
- Group service: remove dead inbound-settings JSON propagation (~120 lines)
  and affected client counts; functions return bare error instead of (int, error)

## gRPC live apply
Resolve tariff inbound IDs and apply diff through Attach/Detach
(AddUser/RemoveUser via Xray AlterInbound gRPC) on group change.

## Fixes
- check_client_ip_job uses client_tariffs.limit_ip_override
- fail2ban IP-limit resolves through tariff_profiles→profiles
- maps.Keys fix for Go 1.23+ iter.Seq in tariffIdsContainingInbound
- listForInboundFiltered includes union-strategy tariff clients
- SyncInbound batch-loads tariff groups, reverts tariff-managed fields
- Tariff values resolved for subscription (userinfo), remark template

## API endpoints (under /panel/api/clients)
Groups: GET/POST /groups, /rename, /delete, /resetTariff, /resetTraffic,
/bulkAdd, /bulkRemove
Tariffs: GET/POST /tariffs, /:id GET/update/delete, /:id/profiles,
POST /tariffs/preview
Profiles: GET/POST /profiles, /:id GET/update/delete
Clients: POST /overrideField, /returnToTariff


